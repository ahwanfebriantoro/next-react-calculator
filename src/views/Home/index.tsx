import Layout from '../../components/HOC/Layout';

interface propsi {
  props: any;
}

const Home: React.FC<propsi> = (props) => {
  return (
    <Layout>
      <div></div>
    </Layout>
  );
};

export default Home;
